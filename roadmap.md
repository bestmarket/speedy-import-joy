# Roadmap

- [x] Import magic-accelerate-flow from GitHub (code, Cloud, schema, media storage bucket, email + Google sign-in, scheduler cron)
- [x] Verify build and pages load (sign-in, Sources, Chat, Studio, Channels — no console errors)
- [x] Verify sign-up creates an account and profile
- [ ] Import existing data — blocked: no CSV/JSON export supplied from the original app's database
- [ ] Scheduled publishing goes live on publish — the 15-minute job targets the production URL, which serves nothing until the app is published
